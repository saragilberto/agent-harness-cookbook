[
  {
    "id": "R01-new-code-compliant",
    "description": "New model extending TenantModel and cache with a tenant prefix. Nothing should be reported, despite the pre-existing legacy violation in the baseline.",
    "baseline": "fixtures/baseline",
    "candidate": "fixtures/candidate-clean",
    "expectViolations": []
  },
  {
    "id": "R02-model-without-isolation",
    "description": "New model extending Model and a cache key without a prefix. Two violations introduced.",
    "baseline": "fixtures/baseline",
    "candidate": "fixtures/candidate-violation",
    "expectViolations": ["A01", "A04"]
  },
  {
    "id": "R03-historical-noise-does-not-count",
    "description": "Baseline compared against itself. Serves as a canary: if this case fails, the finding's fingerprint is unstable and every other case becomes noise.",
    "baseline": "fixtures/baseline",
    "candidate": "fixtures/baseline",
    "expectViolations": []
  }
]
