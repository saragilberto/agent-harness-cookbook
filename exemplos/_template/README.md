# <Title — same title as the article>

Article: <url>

## The problem

<2 to 3 sentences, for someone who arrived via GitHub without reading the
text. State the failure mode this example addresses, not the solution.>

## How to run

```bash
cd exemplos/<article-slug>
node --test
```

<Add any other copyable command needed to exercise the artifact directly,
the way a reader would. No implicit step.>

## What to look at first

<Which file and which function, so nobody is forced to read everything.
Call out the one or two design choices that aren't obvious from the code
alone.>

## Limitations

- <What this example doesn't cover. This section is not optional — an
  example without a declared limitation is an example nobody ran against
  real code.>
